# Fusen

if(WASMEDGE_PLUGIN_WASI_NN_BACKEND)
  string(TOLOWER ${WASMEDGE_PLUGIN_WASI_NN_BACKEND} BACKEND)
  if(BACKEND STREQUAL "ggml")
    if(WASMEDGE_PLUGIN_WASI_NN_GGML_STRATEGY)
      # TODO: select strategy based on GGML_STRATEGY
      if (WASMEDGE_PLUGIN_WASI_NN_GGML_STRATEGY STREQUAL "SPECULATIVE")
      else()
      message("WASMEDGE_PLUGIN_WASI_NN_GGML_STRATEGY=${WASMEDGE_PLUGIN_WASI_NN_GGML_STRATEGY}")
      endif()
    endif()

  endif()
endif()
